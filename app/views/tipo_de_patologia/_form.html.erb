<%= form_for(@tipo_de_patologium) do |f| %>
  <% if @tipo_de_patologium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_de_patologium.errors.count, "error") %> prohibited this tipo_de_patologium from being saved:</h2>

      <ul>
      <% @tipo_de_patologium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
